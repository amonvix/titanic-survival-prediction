name: 🧪 CI - Build, Test and Publish

on:
  push:
    branches:
      - main
  pull_request:
  workflow_dispatch:

permissions:
  contents: read
  packages: write

jobs:
  build-test-publish:
    runs-on: ubuntu-latest

    steps:
      # 1️⃣ Checkout do código
      - name: 📥 Checkout repository
        uses: actions/checkout@v4

      # 2️⃣ Setup Python
      - name: 🐍 Setup Python
        uses: actions/setup-python@v4
        with:
          python-version: '3.10'

      # 3️⃣ Install dependencies
      - name: 📦 Install dependencies
        run: |
          python -m pip install --upgrade pip
          pip install -r requirements.txt

      # 4️⃣ Rodar testes (sem falhar se não houver testes)
      - name: 🧪 Running tests (tolerant if none exist)
        run: |
          echo "⚙️ Checking for tests..."
          if ls tests/*.py 1> /dev/null 2>&1; then
            echo "✅ Tests found. Running pytest..."
            pytest
          else
            echo "⚠️ No tests found. Skipping pytest."
          fi
